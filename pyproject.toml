[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "maktaba"
version = "0.1.0"
description = "Arabic desktop reader and search app built with GTK4"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
maktaba = "main:main"
